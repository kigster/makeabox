<%- unless params['commit'] %>
<div class="row">
  <div class="col-sm-2"></div>
  <div class="col-sm-8 large">
    <p><strong>What is this?</strong> Don't worry, it's very simple! This is a little helper that
      lets you generate PDFs, which you can feed to your laser cutter, and cut some pretty boxes that
      with sides that snap very well together.  Neat-o, right?</p>
    <p>The PDF will be downloaded to your computer, and it's ready for printing on laser cutter,
      or you can import it into Adobe Illustrator for further customization.</p>
  </div>
  <div class="col-sm-2"></div>
</div>
<%- end %>

<div class="row">
  <div class="col-sm-2"></div>
  <div class="col-sm-8">
  <%- if @error %>
      <div class="error">
        Whoops, <%= @error %> :(
      </div>
  <%- end %>
  </div>
  <div class="col-sm-2"></div>
</div>

<%= form_tag("", method: "post", novalidate: true) do %>

    <div class="row">
      <div class="col-sm-2"></div>
      <div class="col-sm-3">
        <h4>Units</h4>

        <p>
          <%= hidden_field_tag("units", @config.units) %>
          <%= label_tag("config[units]_in", "Inches") %>
          <%= radio_button_tag("config[units]", "in", @config.units.eql?('in')) %>
        </p>

        <p>
          <%= label_tag("config[units]_mm", "Millimeters") %>
          <%= radio_button_tag("config[units]", "mm", @config.units.eql?('mm')) %>
        </p>

      </div>
      <div class="col-sm-5">
        <h4>Orientation and Page Size</h4>
        <p>
          <%= label_tag(:page_layout_portrait, "Portrait") %>
          <%= radio_button_tag(:page_layout, "portrait", @config.page_layout == 'portrait') %>
        </p>

        <p>
          <%= label_tag(:page_layout_landscape, "Landscape") %>
          <%= radio_button_tag(:page_layout, "landscape", @config.page_layout == 'landscape') %>
        </p>

        <p>
          <%= label_tag(:page_size, "Page Size") %>&nbsp;<%= select_tag('config[page_size]', options_for_select(@page_size_options, @page_size_options.find { |a| a.first.include?(@config.page_size) })) %>
        </p>

        <p>
          <%= label_tag(:metadata, "Meta Data") %>&nbsp;<%= check_box_tag(:metadata, 1, @config['metadata'] ) %>
        </p>

      </div>
      <div class="col-sm-2"></div>

    </div>

    <div class="row">
      <div class="col-sm-2"></div>
      <div class="col-sm-4">
        <h4>Box Dimensions</h4>
        <%= raw(config_form_element_group(%w(width height depth thickness notch), @config)) %>
      </div>
      <div class="col-sm-4">
        <h4>Optional Settings</h4>
        <%= raw(config_form_element_group(%w(margin padding stroke), @config)) %>
      </div>
      <div class="col-sm-2"></div>
    </div>

    <div class="row">
      <div class="col-sm-5"></div>
      <div class="col-sm-2">
        <div class="control-group">
            <%= submit_tag("Make My Pretty Box", class: 'btn btn-default btn-success') %>
        </div>
      </div>
      <div class="col-sm-5"></div>
    </div>

<% end %>

