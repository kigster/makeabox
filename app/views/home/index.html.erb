<p>What is this site?</p>

<p>It lets you make laser cut boxes!</p>

<p>Just enter the parameters below, click Submit and download a PDF document, ready for printing on laser cutter,
OR ready for importing into Adobe Illustrator for further customization!</p>

<div id="formDiv">
  <%- if @error %>
      <div class="error">Whoops, <%= @error %></div>
  <%- end %>
  <%= form_tag("", method: "post") do %>
      <h4>Units</h4>

      <p>
        <%= hidden_field_tag("units", @config.units) %>
        <%= label_tag("config[units]_in", "Inches") %>
        <%= radio_button_tag("config[units]", "in", @config.units.eql?('in')) %>
      </p>

      <p>
        <%= label_tag("config[units]_mm", "Millimeters") %>
        <%= radio_button_tag("config[units]", "mm", @config.units.eql?('mm')) %>
      </p>

      <h4>Dimensions</h4>
      <p>All dimensions are of the internal box space.  Actual box dimensions
      would be larger by 2 x thickness of the material.</p>
      <p>
        <%= label_tag(:width, "Width") %>
        <%= text_field(:config, :width) %>
      </p>

      <p>
        <%= label_tag(:width, "Height") %>
        <%= text_field(:config, :height) %>
      </p>

      <p>
        <%= label_tag(:width, "Depth") %>
        <%= text_field(:config, :depth) %>
      </p>

      <p>
        <%= label_tag(:width, "Material Thickness") %>
        <%= text_field(:config, :thickness) %>
      </p>

      <p>Notch length is optional – if not provided, it will be automatically chosen.</p>
      <p>
        <%= label_tag(:notch, "Notch Length") %>
        <%= text_field(:config, :notch) %>
      </p>

      <h4>Optional</h4>

      <p>
        <%= label_tag(:notch, "Margins") %>
        <%= text_field(:config, :margin) %>
      </p>

      <p>
        <%= label_tag(:padding, "Padding") %>
        <%= text_field(:config, :padding) %>
      </p>

      <p>
        <%= label_tag(:stroke, "Stroke") %>
        <%= text_field(:config, :stroke) %>
      </p>

      <h4>Orientation</h4>

      <p>
        <%= label_tag(:page_layout_portrait, "Portrait") %>
        <%= radio_button_tag(:page_layout, "portrait", @config.page_layout == 'portrait') %>
      </p>
      <p>
        <%= label_tag(:page_layout_landscape, "Landscape") %>
        <%= radio_button_tag(:page_layout, "landscape", @config.page_layout == 'landscape') %>
      </p>

      <p>
        <%= label_tag(:page_size, "Page Size") %>
        <%= select_tag('config[page_size]', options_for_select(@page_size_options, @page_size_options.find{|a| a.first.include?(@config.page_size)})) %>
      </p>

      <p style="height:50px"></p>
      <%= label_tag(:empty, "") %>
      <%= submit_tag("Make A Box!") %>
  <% end %>

</div>



